// Objeto: Es una agrupación de datos, estos datos deben de hacer sentido entre si.
// Los objetos no tienen variables,
// Los objetos tienen propiedades.

// Objeto vacio
const miPrimerObjeto={}

// Objeto
const miObjeto={
    unNumero: 12,
    unString:'Cadena de caracteres',
    unaCondicion: true,
}

console.log(miObjeto);

// Imprimir solamente una propiedad del objeto
console.log(miObjeto.unString);