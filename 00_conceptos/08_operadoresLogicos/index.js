// Operadores lógicos
// or ||, and &&, not !

// or evalua hasta encontrar el primer true
const resultadoOr = false || false || 'Hola' || 'Evelyn'
console.log(resultadoOr);

// and evalua hasta encontrar el primer false
const resultadoAnd = true && false
console.log(resultadoAnd);

// not
const resultadoNot = !true
console.log(resultadoNot);
