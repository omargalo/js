// window.onload
// Espera a que se carguen todos los elementos html
// antes de ejecutar el codigo JS

window.onload = () => {
    const parrafo = document.getElementById('text')
    console.log(parrafo.innerHTML)
}
