// Array vs List: En list los tipos de datos deben de ser del mismo tipo, no asi en los arrays.

// Arreglo vacio
const arregloVacio=[]

// Arreglo
const arreglo=[1,2,'Hola','Evelyn']

console.log(arregloVacio, arreglo);

// Agregar mas elementos al array
arreglo.push(5)

console.log(arreglo);

// Agregando elementos al array vacio
arregloVacio.push(5)
arregloVacio.push(10)
arregloVacio.push(true)
arregloVacio.push(27)
arregloVacio.push(33)
arregloVacio.push('Evelyn')

console.log(arregloVacio);
